V1.23:
- Scheduled maintenance entries now have 'Realizar' and 'Eliminar programación'.
- 'Realizar' creates a new history entry and automatically creates the next cycle using the prior interval.
- The old scheduled occurrence remains in history but its future schedule is cleared.
- 'Eliminar programación' removes only the future rule; history stays.
- Keeps edit/delete capability for maintenance records.
